- name: REMOTED_ENDPOINT
  description: remoted statistics endpoint
  configuration_parameters:
    ENDPOINT: blackwell-remoted
  metadata:
    endpoint: blackwell-remoted

- name: ANALYSISD_ENDPOINT
  description: analysisd statistics endpoint
  configuration_parameters:
    ENDPOINT: blackwell-analysisd
  metadata:
    endpoint: blackwell-analysisd

- name: BLACKWELL_DB_ENDPOINT
  description: blackwell-db statistics endpoint
  configuration_parameters:
    ENDPOINT: blackwell-db
  metadata:
    endpoint: blackwell-db
