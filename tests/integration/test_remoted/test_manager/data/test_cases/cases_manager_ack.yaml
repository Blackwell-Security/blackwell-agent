- name: Command config blackwell manager
  description: Command config blackwell manager
  configuration_parameters:
    PROTOCOL: 'TCP'
  metadata:
    protocol: 'TCP'

- name: Command config blackwell manager
  description: Command config blackwell manager
  configuration_parameters:
    PROTOCOL: 'UDP'
  metadata:
    protocol: 'UDP'

- name: Command config blackwell manager
  description: Command config blackwell manager
  configuration_parameters:
    PROTOCOL: 'TCP,UDP'
  metadata:
    protocol: 'TCP'

- name: Command config blackwell manager
  description: Command config blackwell manager
  configuration_parameters:
    PROTOCOL: 'TCP,UDP'
  metadata:
    protocol: 'UDP'

- name: Command config blackwell manager
  description: Command config blackwell manager
  configuration_parameters:
    PROTOCOL: 'UDP,TCP'
  metadata:
    protocol: 'UDP'

- name: Command config blackwell manager
  description: Command config blackwell manager
  configuration_parameters:
    PROTOCOL: 'UDP,TCP'
  metadata:
    protocol: 'TCP'
